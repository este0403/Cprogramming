// ********************************************
//   제목 : 포인터 덧셈연산을 이용한 배열의 접근 프로그램 (교재 300p 문제2 변형)
//   날짜 : 2026년 9월 10일
//   작성자 : 2600152 이준영
// ********************************************

/*
[원본 문제2]
문제1에서는 포인터 변수 ptr에 저장된 값을 변경시켜가면서 배열요소에
접근하였다. 이번에는 ptr에 저장된 값 자체는 변경시키지 않고, ptr을
대상으로 덧셈연산을 하여 그 결과로 반환되는 주소 값을 통해서 모든
배열요소에 접근하여 값을 2씩 증가시킨다.

[변형한 문제]
길이가 5인 double형 배열 arr을 선언하고, 키보드로부터 5개의 실수를
입력받아 초기값으로 저장한다(저장할 때도 ptr의 값 자체는 바꾸지 않고
ptr+i 형태의 덧셈연산을 이용). 그 다음 문제2와 동일하게 ptr의 값을
바꾸지 않고 덧셈연산(ptr+i)으로 모든 배열요소에 접근하여 값을 2씩
증가시킨다. 추가로 예제11처럼 "값을 2씩 증가시키는 연산"을 5가지
방식(배열표현, 배열명+주소표현, 포인터 인덱스, 포인터+주소표현,
증감연산자)으로 모두 작성해본다.
*/

#include <stdio.h>

int main(void)
{
    double arr[5];
    double* ptr = arr;   // 배열의 첫번째 요소를 가리키는 포인터 변수 ptr
    int i;

    // 키보드로부터 5개의 실수를 입력받아 저장
    // ptr의 값 자체는 바꾸지 않고, ptr+i 형태의 덧셈연산으로 위치를 계산함
    for (i = 0; i < 5; i++)
    {
        printf("실수를 입력하시오: ");
        scanf("%lf", (ptr + i));
    }

    printf("증가 전 배열 값: ");
    for (i = 0; i < 5; i++)
        printf("%.1f ", *(ptr + i));
    printf("\n\n");

    // ---- 방법1 : 배열표현 a[i] ----
    for (i = 0; i < 5; i++)
        arr[i] += 2;
    printf("[방법1: a[i]]   ");
    for (i = 0; i < 5; i++) printf("%.1f ", arr[i]);
    printf("\n");
    for (i = 0; i < 5; i++) arr[i] -= 2;   // 다음 방법 시연을 위해 원상복구

    // ---- 방법2 : 배열명을 이용한 주소표현 *(a+i) ----
    for (i = 0; i < 5; i++)
        *(arr + i) += 2;
    printf("[방법2: *(a+i)] ");
    for (i = 0; i < 5; i++) printf("%.1f ", *(arr + i));
    printf("\n");
    for (i = 0; i < 5; i++) arr[i] -= 2;

    // ---- 방법3 : 포인터변수를 배열처럼 사용 p[i] ----
    for (i = 0; i < 5; i++)
        ptr[i] += 2;
    printf("[방법3: p[i]]   ");
    for (i = 0; i < 5; i++) printf("%.1f ", ptr[i]);
    printf("\n");
    for (i = 0; i < 5; i++) arr[i] -= 2;

    // ---- 방법4 : 포인터를 이용한 주소표현 *(p+i)  (문제2가 요구하는 방식) ----
    //      ptr 자체의 값은 변경하지 않고, 덧셈연산(p+i)의 결과 주소로만 접근
    for (i = 0; i < 5; i++)
        *(ptr + i) += 2;
    printf("[방법4: *(p+i)] ");
    for (i = 0; i < 5; i++) printf("%.1f ", *(ptr + i));
    printf("\n");
    for (i = 0; i < 5; i++) arr[i] -= 2;

    // ---- 방법5 : 증감연산자 이용 *p++  (참고용, ptr 값이 실제로 변경됨) ----
    for (i = 0; i < 5; i++)
        *ptr++ += 2;
    ptr = arr;   // 증가연산으로 이동한 ptr을 다시 첫번째 요소로 복구
    printf("[방법5: *p++]   ");
    for (i = 0; i < 5; i++) printf("%.1f ", *(ptr + i));
    printf("\n");

    return 0;
}

/* 결과: (1.5, 2.5, 3.5, 4.5, 5.5 입력 시)
실수를 입력하시오: 1.5
실수를 입력하시오: 2.5
실수를 입력하시오: 3.5
실수를 입력하시오: 4.5
실수를 입력하시오: 5.5
증가 전 배열 값: 1.5 2.5 3.5 4.5 5.5

[방법1: a[i]]   3.5 4.5 5.5 6.5 7.5
[방법2: *(a+i)] 3.5 4.5 5.5 6.5 7.5
[방법3: p[i]]   3.5 4.5 5.5 6.5 7.5
[방법4: *(p+i)] 3.5 4.5 5.5 6.5 7.5
[방법5: *p++]   3.5 4.5 5.5 6.5 7.5
*/
