// **********************************************
// 제 목 : 실습과제2
// 날 짜 : 2026년 9월 22일
// 작성자 : 2600152 이준영
// **********************************************
#include <stdio.h>

// 원래 코드는 value를 값으로 전달받아(값에 의한 호출) 함수 안에서만
// value가 증가하고, main함수의 number는 전혀 바뀌지 않는 문제가 있었음
// -> 함수 호출시 number의 "값"이 복사되어 value라는 별도의 지역변수에
//    저장되므로, add2함수 내부에서 value를 바꿔도 number가 저장된
//    메모리 공간에는 아무런 영향을 주지 못함 (17페이지 메모리 그림 참고)
// -> 해결책: number의 "주소"를 넘겨서(주소에 의한 호출) 함수 내부에서
//    포인터를 통해 number가 저장된 메모리 공간을 직접 수정하도록 함
void add2(int* pvalue);

int main(void)
{
    int number;

    printf("정수를 입력하세요:");
    scanf("%d", &number);

    add2(&number);              // number의 주소를 전달 (주소에 의한 호출)

    printf("2만큼 증가한 값:%d\n", number);

    return 0;
}

// 매개변수를 포인터(int*)로 선언 -> main의 number를 가리키는 주소를 받음
void add2(int* pvalue)
{
    *pvalue += 2;   // *pvalue는 number가 저장된 메모리 공간 자체를 의미
                    // 따라서 이 연산은 number의 값을 직접 2만큼 증가시킴
}

/* 실행결과
정수를 입력하세요:15
2만큼 증가한 값:17
*/
